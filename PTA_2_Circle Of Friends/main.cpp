#include<bits/stdc++.h>

using namespace std;

int a[30001];
int main()
{
    char c;
    int temp,m,i=1,n;
    cin>>n>>m;
    while(i<=m)
    {
        scanf("%d%c",&temp,&c);
        if(a[temp]==0)a[temp]=i;
        else l
        if(c=='\n')i++;
    }
    return 0;
}
