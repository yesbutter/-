#include <bits/stdc++.h>

using namespace std;

int funa(int n)
{
    int sum=1;
    for(int i=2;i<=n;i++)
        sum*=i;
    return sum;
}
int main()
{
    int T,a,b;
    long long temp;
    long double win;
    while(T--)
    {
        cin>>a>>b;
        win=
    }
    return 0;
}
